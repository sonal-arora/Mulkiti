<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">

        <record forcecreate="True" id="ir_cron_document_expiry_notification" model="ir.cron">
            <field name="name">Document Expiry Notification</field>

            <!-- FIX: Ensure correct external ID -->
            <field name="model_id" ref="hr_employee_custom.model_employee_document"/>

            <field name="state">code</field>
            <field name="code">model.cron_expiry_notification()</field>

            <!-- Run Daily -->
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>

        </record>

    </data>
</odoo>